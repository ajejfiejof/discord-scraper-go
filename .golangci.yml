run:
  timeout: 5m
linters:
  enable:
    - govet
    - errcheck
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam
    - gocritic
version: "2"
formatters:
  enable:
    - gofmt
    - goimports
